{
  "name": "companion-module-bpm2osc",
  "version": "1.0.1",
  "description": "Bitfocus Companion module for BPM2OSC",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -p tsconfig.json --watch",
    "dev": "tsc -p tsconfig.json --watch",
    "package": "companion-module-build"
  },
  "dependencies": {
    "@companion-module/base": "^1.14.1",
    "eventsource": "^3.0.2"
  },
  "devDependencies": {
    "@companion-module/tools": "^2.0.0",
    "@types/node": "^18.0.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": "^22.20"
  }
}
